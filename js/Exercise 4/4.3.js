function multiRand(n) {
  return Math.floor(Math.random()*number);
}

function rand10 (min, max) {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}
var min = (1);
var max = (10);
var n = (10);
var number =(1);

console.log(multiRand(n) + rand10 (min, max));




